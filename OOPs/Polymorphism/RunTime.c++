#include<bits/stdc++.h>
using namespace std;
int main(){
    int x = 10;
    int y;
    cout<<"Enter your no: ";
    cin>>y;  // if enter 0 then throw the run time error
    int  z = x/y; 
    cout<<"Output :"<<z;  // thow run time error

  return 0;
}